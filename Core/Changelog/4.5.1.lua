local TXUI, F, E, I, V, P, G = unpack((select(2, ...)))

TXUI.Changelog["4.5.1"] = {
  HOTFIX = true,
  CHANGES = {
    "* ElvUI",
    " Fixed Action Bars shenanigans",
    " Fixed Focus Frame Y offset",
    " Fixed health format",
  },
}
