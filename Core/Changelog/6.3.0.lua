local TXUI, F, E, I, V, P, G = unpack(select(2, ...))

TXUI.Changelog["6.3.0"] = {
  HOTFIX = true,
  CHANGES = {
    "* ToxiUI",
    "TBD",
  },
  CHANGES_CLASSIC = {
    "Release ToxiUI Classic",
  },
}
