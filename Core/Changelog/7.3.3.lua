local TXUI, F, E, I, V, P, G = unpack((select(2, ...)))

TXUI.Changelog["7.3.3"] = {
  HOTFIX = true,
  CHANGES = {
    "* Bug fixes",
    "Fix incorrect gradient fade direction for party and raid frames",
    "Display labels for additional hearthstone flyout",
  },
}
