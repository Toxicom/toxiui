local TXUI, F, E, I, V, P, G = unpack((select(2, ...)))

TXUI.Changelog["6.9.7"] = {
  HOTFIX = true,
  CHANGES = {
    "* Bug fixes",
    "Fix Mists of Pandaria not being recognized by Curseforge or Wago",
  },
}
