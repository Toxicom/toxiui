local TXUI, F, E, I, V, P, G = unpack((select(2, ...)))

TXUI.Changelog["7.5.3"] = {
  HOTFIX = true,
  RELEASE_DATE = "Aug 21, 2026",
  CHANGES = {
    "* Bug fixes",
    F.String.Retail() .. "More secret errors -.-",
  },
}
