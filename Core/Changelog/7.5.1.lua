local TXUI, F, E, I, V, P, G = unpack((select(2, ...)))

TXUI.Changelog["7.5.1"] = {
  HOTFIX = true,
  RELEASE_DATE = "Aug 13, 2026",
  CHANGES = {
    "* Bug fixes",
    "Even more secret protection for gradients",
  },
}
