local TXUI, F, E, I, V, P, G = unpack((select(2, ...)))

TXUI.Changelog["4.1.1"] = {
  CHANGES = {
    "* Profile updates",
    " This time really change color for Quest Tracker",
    " Force enable Chat module",
  },
}
