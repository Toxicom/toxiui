local TXUI, F, E, I, V, P, G = unpack((select(2, ...)))

TXUI.Changelog["6.7.4"] = {
  HOTFIX = true,
  CHANGES = {
    "* Bug fixes",
    "Fix DataBar for Cataclysm",
    "Fix Currency module's Shift-RightClick",
  },
}
