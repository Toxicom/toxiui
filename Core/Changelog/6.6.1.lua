local TXUI, F, E, I, V, P, G = unpack((select(2, ...)))

TXUI.Changelog["6.6.1"] = {
  HOTFIX = true,
  CHANGES = {
    "* New features",
    "Additional scaling for Classic Taxi Frame",

    "* Bug fixes",
    "Fix AddOn not loading for Vanilla",
    "Fix version mismatches",
  },
}
