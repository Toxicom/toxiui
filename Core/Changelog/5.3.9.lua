local TXUI, F, E, I, V, P, G = unpack(select(2, ...))

TXUI.Changelog["5.3.9"] = {
  HOTFIX = true,
  CHANGES = {
    "* Misc",
    " Fix previously added " .. F.String.ElvUI("Broker Translocation Matrix") .. " hearthstone",
  },
}
