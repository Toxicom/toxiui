local TXUI, F, E, I, V, P, G = unpack(select(2, ...))

TXUI.Changelog["6.2.3"] = {
  HOTFIX = true,
  CHANGES = {
    "* ToxiUI",
    "Increase minimum required ElvUI version",
  },
  CHANGES_WRATH = {
    "Update for 3.4.2",
  },
}
