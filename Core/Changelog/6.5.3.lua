local TXUI, F, E, I, V, P, G = unpack((select(2, ...)))

TXUI.Changelog["6.5.3"] = {
  HOTFIX = true,
  CHANGES = {
    "* Development improvements",
    "Hotfix to enable a certain chat command",
  },
}
